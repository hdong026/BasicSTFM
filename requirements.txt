# Recommended environment:
#   conda create -n basicstfm python=3.10 -y
#
# PyTorch note:
#   torch==2.6.0 is intentionally pinned to avoid accidentally installing
#   newer CUDA wheels that may require a newer NVIDIA driver.
#
# Runtime dependencies
numpy>=1.23
h5py>=3.8
pandas>=1.5
matplotlib>=3.7
PyYAML>=6.0
tables>=3.8
torch==2.6.0
tqdm>=4.66

# Development and verification
pytest>=7.4
ruff>=0.4
