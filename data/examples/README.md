# Data Examples

This directory is reserved for lightweight example metadata or toy files.

Large datasets should not be committed. Put benchmark files under:

```text
data/<DATASET_NAME>/
```

For a synthetic run, no files are required because `SyntheticDataModule` generates data in memory.
